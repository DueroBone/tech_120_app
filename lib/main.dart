import 'package:flutter/material.dart'; // Where flutter finds what makes the app
import 'package:tech_120_app/Views/Manual/home_page_view.dart'; // and home page

void main() { // Starts the execution of the app, no touch
  runApp(const FigmaToCodeApp());
  print("App Started");
}

// Generated by: https://www.figma.com/community/plugin/842128343887142055/
class FigmaToCodeApp extends StatelessWidget {
  const FigmaToCodeApp({super.key}); // Complicated, hopefully no touch

  @override
  Widget build(BuildContext context) {
    return MaterialApp( // The base of the app
      theme: ThemeData.light().copyWith(
        scaffoldBackgroundColor: const Color.fromARGB(255, 0, 0, 0), // Background color
        appBarTheme: const AppBarTheme(
          backgroundColor: Color.fromARGB(255, 64, 128, 196), // Color for the top bar
        ),
      ),
      home: HomePageView(), // Load the home page
    ); // ctrl + click to view
  }
}
